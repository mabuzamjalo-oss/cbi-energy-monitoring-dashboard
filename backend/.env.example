# Copy this file to .env and fill in your real Postgres credentials.
# If .env is missing / DATABASE_URL is unset, the app falls back to a local
# SQLite file (cbi_energy_dev.db) so you can run it immediately.

DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/cbi_energy
